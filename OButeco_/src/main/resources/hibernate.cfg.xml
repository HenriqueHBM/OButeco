<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>

        <!-- Driver -->
        <property name="hibernate.connection.driver_class">
            org.postgresql.Driver
        </property>

        <!-- URL do banco -->
        <property name="hibernate.connection.url">
            jdbc:postgresql://localhost:5432/bd_obuteco
        </property>

        <!-- Usuário -->
        <property name="hibernate.connection.username">
            root
        </property>

        <!-- Senha -->
        <property name="hibernate.connection.password">
            password;
        </property>

        <!-- Dialeto (Hibernate 6) -->
        <property name="hibernate.dialect">org.hibernate.dialect.PostgreSQLDialect</property>

        <!-- Atualização automática do schema -->
        <property name="hibernate.hbm2ddl.auto">none</property>

        <!-- Logs SQL -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>

        <!-- Mapeamento das entidades -->
        <!-- Exemplo -->
        <!-- <mapping class="com.exemplo.model.Produto"/> -->
        <mapping class="buteco.model.produto.Produto" />
        <mapping class="buteco.model.produto.Categoria" />
        <mapping class="buteco.model.produto.Conversoes" />

    </session-factory>
</hibernate-configuration>